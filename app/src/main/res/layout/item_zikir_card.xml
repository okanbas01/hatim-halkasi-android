<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white"
    app:strokeColor="#E8F5E9"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Üst: Sürükle + İsim (üstte) + X/hedef (altında) + Çöp + Menü -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="14dp">

            <ImageView
                android:id="@+id/imgDrag"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@android:drawable/ic_menu_sort_by_size"
                app:tint="@color/text_grey"
                android:contentDescription="Sırala"/>
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_marginStart="12dp">
                <TextView
                    android:id="@+id/txtZikirName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="17sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_dark"
                    android:ellipsize="end"
                    android:maxLines="1"/>
                <TextView
                    android:id="@+id/txtCountTarget"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="14sp"
                    android:textColor="@color/text_grey"
                    android:maxLines="2"
                    android:layout_marginTop="2dp"/>
            </LinearLayout>
            <ImageButton
                android:id="@+id/btnDelete"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_delete"
                app:tint="#EF5350"
                android:contentDescription="Sil"/>
            <ImageButton
                android:id="@+id/btnEdit"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_edit"
                app:tint="@color/text_grey"
                android:contentDescription="Düzenle"/>
        </LinearLayout>

        <!-- Alt: Eksi (boşluk) | Büyük sayaç (dokun = artır + titreşim) | (boşluk) | Sıfırla -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Eksi - yuvarlak, kendi alanında -->
            <FrameLayout
                android:id="@+id/btnMinus"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_zikir_btn_light"
                android:foreground="@drawable/bg_zikir_circle_ripple"
                android:clickable="true"
                android:focusable="true">
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:text="−"
                    android:textSize="24sp"
                    android:textColor="@color/white"
                    android:gravity="center"/>
            </FrameLayout>

            <View android:layout_width="16dp" android:layout_height="1dp"/>

            <!-- Büyük sayaç - yuvarlak yeşil, dokununca artır (büyük tıklanabilir alan) -->
            <FrameLayout
                android:id="@+id/layoutCount"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:minHeight="152dp"
                android:gravity="center"
                android:background="@drawable/bg_zikir_big_circle_ripple"
                android:clickable="true"
                android:focusable="true">
                <View
                    android:layout_width="136dp"
                    android:layout_height="136dp"
                    android:background="@drawable/bg_zikir_big_circle_light"/>
                <TextView
                    android:id="@+id/txtCount"
                    android:layout_width="136dp"
                    android:layout_height="136dp"
                    android:gravity="center"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:text="0"/>
            </FrameLayout>

            <View android:layout_width="16dp" android:layout_height="1dp"/>

            <!-- Sıfırla - yuvarlak, metin ikon (crash önlemi) -->
            <FrameLayout
                android:id="@+id/btnReset"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_zikir_btn_light"
                android:foreground="@drawable/bg_zikir_circle_ripple"
                android:clickable="true"
                android:focusable="true">
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:text="↻"
                    android:textSize="22sp"
                    android:textColor="@color/white"
                    android:gravity="center"/>
            </FrameLayout>
        </LinearLayout>

        <TextView
            android:id="@+id/btnSave"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="10dp"
            android:text="Geçmişe kaydet"
            android:textSize="12sp"
            android:textColor="@color/primary_green"
            android:padding="6dp"
            android:background="?attr/selectableItemBackground"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
