<?xml version="1.0" encoding="utf-8"?>
<!-- Uygulama açılırken boş beyaz sayfada logo ortada görünsün (sistem splash / tema arka planı). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/bg_intro" />
        </shape>
    </item>
    <item
        android:width="200dp"
        android:height="200dp"
        android:gravity="center"
        android:drawable="@drawable/app_logo_final" />
</layer-list>
